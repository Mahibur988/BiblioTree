# BiblioTree
BiblioTree is a C++ project that simulates a free library system using a Binary Search Tree (BST).   It reads a tab-separated inventory of books and organizes them alphabetically by title for efficient storage and retrieval.
